package com.shyam.kamak.godown.config;

public class config {

    public enum BundleStatus { AVAILABLE, RESERVED, SOLD }
    public enum DiscountType { NONE, FLAT, PERCENTAGE }
    public enum TaxType { NONE, GST, VAT, IGST, CGST_SGST }
}
